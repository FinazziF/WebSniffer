@page "/Interface/{ip}"
@model WebSniffer.Pages.InterfaceModel
@{
}

<component type="typeof(TrafficInterface)" param-ip="@Model.GetIp()" render-mode="ServerPrerendered" />
@*@(await Html.RenderComponentAsync<WebSniffer.Pages.TrafficInterface>( RenderMode.ServerPrerendered , new { ip = Model.ip }))*@